# Windows SDK requirements that a servicing REVISION decides, not the version.
#
# The SDK's directories and registry keys are named after the major build --
# "10.0.26100.0" -- and carry no servicing revision at all. So
# Include/10.0.26100.0/um existing is a true statement that answers nothing:
# revision 4654 and revision 7705 are indistinguishable by presence. That is
# how the sixth Windows defect got through a preflight that checks every
# prerequisite directory. This file is the revision axis, asserted separately.
#
# Three whitespace-separated columns. Lines starting with # are comments.
# "<version>" in a path is replaced with build/WINDOWS_SDK_VERSION.
#
#   symbol  <directory>  <identifier>
#       The identifier must appear in some .h file under that directory. Used
#       where the revision is legible only from the headers' contents, which is
#       the normal case: headers carry no version resource.
#
#   libsymbol <library>  <identifier>
#       The library must define the identifier. A .lib carries its symbol names
#       as plain strings, so this needs no toolchain. Required wherever a header
#       DECLARES something a library DEFINES, because those two can come from
#       different revisions and the compile will not notice.
#
#   version <file>  <minimum>
#       The file's FileVersion must be >= the minimum, compared as a dotted
#       version. Used where a real version resource exists.
#
# SOURCES
#
# .workspace/src/docs/windows_build_instructions.md:54-56 requires
#   "[Windows 11 SDK] version 10.0.26100.7705", and build/WINDOWS_SDK_INSTALLER_*
#   pins that exact release. Chromium names a revision, not a floor, and the
#   pin travels with build/CHROMIUM_VERSION.
#
# .workspace/src/docs/windows_build_instructions.md:57-59 requires
#   "(Windows 11) SDK Debugging Tools 10.0.26100.3323 or higher. This version of
#   the Debugging tools is needed in order to support reading the large-page
#   PDBs that Chrome uses to allow greater-than 4 GiB PDBs." That one IS a
#   floor, and dbghelp.dll carries a version resource, so it is checked
#   numerically rather than by symbol.
#
# IUIAutomationClientInfoSource and IUIAutomationClientInfo are used
#   unconditionally by ui/accessibility/platform/uia_client_info_source_win.cc
#   (lines 24, 30, 56, 66) with no preprocessor guard and no __has_include, so
#   an older 26100 revision is a hard compile error rather than a silently
#   disabled feature. Measured: the windows-x64 gate reached 26,642 of 33,797
#   edges on SDK 10.0.26100.4654 and failed with
#   "error: unknown type name 'IUIAutomationClientInfo'".
#
#   Both are confirmed present in 10.0.26100.7705 and confirmed absent from an
#   earlier 26100 revision, by reading the official Microsoft.Windows.SDK.CPP
#   NuGet packages for each -- the same headers the installer lays down, so the
#   pin is verified against the bytes rather than against the release notes, and
#   without a Windows runner. Both live in um/UIAutomationCore.h, which is why
#   these rows name the `um` DIRECTORY: a row pinned to um/UIAutomationClient.h,
#   the header the interface names, would have failed on a correct SDK.
#
#   The UIA requirement is header AND library, and the library half is the one
#   that would have cost the most. um/UIAutomationCore.h only DECLARES
#   "EXTERN_C const CLSID CLSID_CUIAutomationClientInfoSource"; the definition
#   lives in a library, found in the pinned package's c/um/x64/Uuid.Lib, and
#   uia_client_info_source_win.cc:144 passes that CLSID to CoCreateInstance. So
#   an overlay or an install that refreshed only the headers would COMPILE
#   clean and fail at LINK -- and the series compile gate never links, so the
#   gate would have gone green and the four-target build would have failed
#   afterwards at four times the price. That is the defect class this whole
#   sequence has been about: a check that passes because it does not reach the
#   thing that breaks. Hence the libsymbol row.
#
# EXPERIMENTAL_IDCompositionDevice6 WAS listed here and should not have been.
#   It is used unconditionally by ui/gl/direct_composition_support.cc:788,
#   ui/gl/dc_layer_tree.cc:147 and ui/gl/dc_layer_tree.h:486, and searching the
#   checkout for a declaration found none, so it looked like an SDK symbol. It
#   is not in any Windows SDK: checked 26100.4188, .7705, .8249, .9169 and
#   28000.2705, absent from all five. It comes from a vendored header,
#   third_party/microsoft_dxheaders/src/include/experimental-composition/
#   experimental-dcomp.h, included by name at direct_composition_support.cc:20.
#
#   The search missed it because third_party/microsoft_dxheaders/src is a
#   nested DEPS sub-repository that this mapping checkout has not synced, so the
#   directory is empty here and present on any runner that ran gclient sync.
#   THE LESSON, because it will recur: a negative result from searching a
#   partial checkout is not evidence of absence. The row would have failed on a
#   correct 10.0.26100.7705 SDK and broken the gate for a reason that did not
#   exist. Rows here must name a symbol the SDK owns; a symbol the checkout
#   supplies belongs to the checkout's pins, not to this table.
#
# IDCompositionDevice2, IDCompositionDevice3 and IDCompositionDevice4 are used
#   unconditionally in the same files and are NOT listed: all three predate
#   26100, and build/vs_toolchain.py only bothers checking for
#   IDCompositionDevice4 when the SDK's major build is <= 22621.
#
# ADDING A ROW
#
# A row belongs here when a build input's adequacy depends on an SDK servicing
# revision and presence cannot tell you. Cite the consumer and, if it exists,
# the run that found it. Prefer a `version` row when a version resource exists,
# because a number that can be compared beats a symbol that has to be chosen.
# Confirm the symbol IS in the pinned SDK before adding it -- the NuGet package
# above makes that a 30-second check from any host -- because an assertion that
# fails on a correct toolchain is worse than no assertion at all. And if a
# header DECLARES what a library DEFINES, add both rows: only the compile
# notices the header, and only the link notices the library.
#
# The list cannot be completed by reading. An unconditional use of a too-new
# interface is a compile error in whichever translation unit uses it, so the
# authoritative enumeration is a build, not a search. What this file buys is
# that each one is found once and then asserted in seconds forever.

symbol	Include/<version>/um	IUIAutomationClientInfoSource
symbol	Include/<version>/um	IUIAutomationClientInfo
libsymbol	Lib/<version>/um/x64/uuid.lib	CLSID_CUIAutomationClientInfoSource
version	Debuggers/x64/dbghelp.dll	10.0.26100.3323
