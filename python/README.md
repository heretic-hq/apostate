# Apostate Python package

This package exposes the Patchright-compatible Python launch API for the
Apostate native Chromium release. It ships the deterministic profile catalogue
and schema as package data; native browser artifacts are verified from a
release manifest before extraction and are not bundled in this source package.
