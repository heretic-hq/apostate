{
  "chromium": "79460ebecaa5625e57a5fb679a735659e73dc687",
  "dawn": "ab8827bc57b176eeaa89f71324130c02d4d41145",
  "edits": [
    {
      "path": "third_party/dawn/include/dawn/native/DawnNative.h",
      "before": "    bool beginCaptureOnStartup = false;\n\n    WGPULoggingCallbackInfo",
      "after": "    bool beginCaptureOnStartup = false;\n    // Native-only provider restriction. Requires one absolute POSIX directory\n    // in additionalRuntimeSearchPaths; no module/system fallback is appended.\n    // This does not enable unsafe APIs or change adapter features/limits.\n    bool onlyUseSwiftShader = false;\n\n    WGPULoggingCallbackInfo"
    },
    {
      "path": "third_party/dawn/src/dawn/native/DawnNative.cpp",
      "before": "                    backendValidationLevel, beginCaptureOnStartup) ==\n               std::tie(rhs.additionalRuntimeSearchPathsCount, rhs.additionalRuntimeSearchPaths,\n                        rhs.platform, rhs.backendValidationLevel, rhs.beginCaptureOnStartup);",
      "after": "                    backendValidationLevel, beginCaptureOnStartup, onlyUseSwiftShader) ==\n               std::tie(rhs.additionalRuntimeSearchPathsCount, rhs.additionalRuntimeSearchPaths,\n                        rhs.platform, rhs.backendValidationLevel, rhs.beginCaptureOnStartup,\n                        rhs.onlyUseSwiftShader);"
    },
    {
      "path": "third_party/dawn/src/dawn/native/Instance.h",
      "before": "    const std::vector<std::string>& GetRuntimeSearchPaths() const;\n",
      "after": "    const std::vector<std::string>& GetRuntimeSearchPaths() const;\n    bool IsSwiftShaderOnly() const { return mOnlyUseSwiftShader; }\n"
    },
    {
      "path": "third_party/dawn/src/dawn/native/Instance.h",
      "before": "    std::vector<std::string> mRuntimeSearchPaths;\n",
      "after": "    std::vector<std::string> mRuntimeSearchPaths;\n    bool mOnlyUseSwiftShader = false;\n"
    },
    {
      "path": "third_party/dawn/src/dawn/native/Instance.cpp",
      "before": "        mBeginCaptureOnStartup = dawnDesc->beginCaptureOnStartup;\n",
      "after": "        mBeginCaptureOnStartup = dawnDesc->beginCaptureOnStartup;\n        mOnlyUseSwiftShader = dawnDesc->onlyUseSwiftShader;\n"
    },
    {
      "path": "third_party/dawn/src/dawn/native/Instance.cpp",
      "before": "    // Default paths to search are next to the shared library, next to the executable, and\n    // no path (just libvulkan.so).\n    if (auto p = GetModuleDirectory()) {\n        mRuntimeSearchPaths.push_back(std::move(*p));\n    }\n    if (auto p = GetExecutableDirectory()) {\n        mRuntimeSearchPaths.push_back(std::move(*p));\n    }\n    mRuntimeSearchPaths.push_back(\"\");",
      "after": "    // The restricted provider uses only the caller's trusted directory.\n    // Ordinary instances retain the complete upstream search policy.\n    if (!mOnlyUseSwiftShader) {\n        if (auto p = GetModuleDirectory()) {\n            mRuntimeSearchPaths.push_back(std::move(*p));\n        }\n        if (auto p = GetExecutableDirectory()) {\n            mRuntimeSearchPaths.push_back(std::move(*p));\n        }\n        mRuntimeSearchPaths.push_back(\"\");\n    }"
    },
    {
      "path": "third_party/dawn/src/dawn/native/Instance.cpp",
      "before": "    BackendsBitset backendsToFind;\n    if (options.Has<RequestAdapterWebGPUBackendOptions>()) {",
      "after": "    BackendsBitset backendsToFind;\n    if (mOnlyUseSwiftShader) {\n        // Fail before loading any backend when provenance is incomplete. Keep\n        // the instance valid so callers receive normal adapter unavailability.\n        if (mRuntimeSearchPaths.size() != 1 || mRuntimeSearchPaths.front().empty() ||\n            mRuntimeSearchPaths.front().front() != '/' ||\n            mRuntimeSearchPaths.front().back() != '/') {\n            ConsumedErrorAndWarnOnce(DAWN_VALIDATION_ERROR(\n                \"SwiftShader-only discovery requires one absolute directory.\"));\n            return {};\n        }\n        if (options.Has<RequestAdapterWebGPUBackendOptions>() ||\n            (options->backendType != wgpu::BackendType::Undefined &&\n             options->backendType != wgpu::BackendType::Vulkan)) {\n            return {};\n        }\n        backendsToFind.set(wgpu::BackendType::Vulkan);\n    } else if (options.Has<RequestAdapterWebGPUBackendOptions>()) {"
    },
    {
      "path": "third_party/dawn/src/dawn/native/vulkan/BackendVk.cpp",
      "before": "    for (ICD icd : kICDs) {\n#if DAWN_PLATFORM_IS(MACOS)",
      "after": "    for (ICD icd : kICDs) {\n        // Unlike forceFallbackAdapter alone, this cannot open a system ICD\n        // after software initialization fails or Core is unavailable.\n        if (instance->IsSwiftShaderOnly() && icd != ICD::SwiftShader) {\n            continue;\n        }\n#if DAWN_PLATFORM_IS(MACOS)"
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "#include \"base/auto_reset.h\"",
      "after": "#include \"base/apostate/profile.h\"\n#include \"base/auto_reset.h\""
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "#include \"gpu/command_buffer/service/isolation_key_provider.h\"",
      "after": "#include \"gpu/command_buffer/service/isolation_key_provider.h\"\n#include \"gpu/command_buffer/service/profile_software_webgpu_policy.h\""
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "#include \"ui/gl/gl_context_egl.h\"",
      "after": "#include \"ui/gl/gl_context_egl.h\"\n#include \"ui/gl/gl_implementation.h\""
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "  bool webgpu_on_vk_gl_interop_ = false;",
      "after": "  bool webgpu_on_vk_gl_interop_ = false;\n  bool profile_software_webgpu_ = false;"
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "  dawn_instance_ = DawnInstance::Create(dawn_platform_.get(), gpu_preferences,\n                                        safety_level_);\n\n",
      "after": ""
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "  DawnProcTable wire_procs = dawn::native::GetProcs();",
      "after": "  dawn::native::DawnInstanceDescriptor provider_descriptor;\n#if BUILDFLAG(IS_LINUX)\n  if (const auto* profile = base::apostate::Profile::Get(); profile && profile->ua_platform()) {\n    profile_software_webgpu_ = CanUseProfileSoftwareWebGPU({\n        .platform = *profile->ua_platform(),\n        .angle_is_swiftshader = gl::GetANGLEImplementation() == gl::ANGLEImplementation::kSwiftShader,\n        .shared_context_is_gl = shared_context_state_ && shared_context_state_->GrContextIsGL(),\n        .safe = safety_level_ == SafetyLevel::kSafe,\n        .has_dawn_overrides = !require_enabled_toggles_.empty() ||\n                              !require_disabled_toggles_.empty() || !runtime_unsafe_features_.empty(),\n        .force_compat = force_webgpu_compat_,\n        .requested_adapter = use_webgpu_adapter_,\n    });\n  }\n#endif\n  provider_descriptor.onlyUseSwiftShader = profile_software_webgpu_;\n  if (profile_software_webgpu_) {\n    // Real CPU devices use the existing checked shared-image staging path.\n    // This changes selection/admission only, never identity or limits.\n    use_webgpu_adapter_ = WebGPUAdapterName::kSwiftShader;\n  }\n  dawn_instance_ = DawnInstance::Create(dawn_platform_.get(), gpu_preferences,\n                                      safety_level_, &provider_descriptor);\n\n  DawnProcTable wire_procs = dawn::native::GetProcs();"
    },
    {
      "path": "gpu/command_buffer/service/webgpu_decoder_impl.cc",
      "before": "    if (use_blocklist() && IsWebGPUAdapterBlocklisted(adapter).blocked) {",
      "after": "    const auto blocklist_options =\n        ProfileSoftwareWebGPUBlocklistOptions(profile_software_webgpu_, adapter_info);\n    if (use_blocklist() && IsWebGPUAdapterBlocklisted(adapter, blocklist_options).blocked) {"
    },
    {
      "path": "gpu/command_buffer/service/BUILD.gn",
      "before": "      \"webgpu_decoder_impl.cc\",\n",
      "after": "      \"profile_software_webgpu_policy.h\",\n      \"webgpu_decoder_impl.cc\",\n"
    },
    {
      "path": "gpu/config/webgpu_blocklist_unittest.cc",
      "before": "#include \"gpu/config/webgpu_blocklist_impl.h\"",
      "after": "#include \"gpu/config/webgpu_blocklist_impl.h\"\n#include \"gpu/command_buffer/service/profile_software_webgpu_policy.h\"\n#include \"third_party/dawn/include/dawn/native/DawnNative.h\""
    },
    {
      "path": "gpu/config/webgpu_blocklist_unittest.cc",
      "before": "}  // namespace gpu\n",
      "after": "#if BUILDFLAG(IS_LINUX)\nTEST(ProfileSoftwareWebGPUPolicyTest, RequiresProfileAndActualSoftwareContext) {\n  webgpu::ProfileSoftwareWebGPUContext context;\n  context.platform = \"Windows\";\n  context.angle_is_swiftshader = true;\n  context.shared_context_is_gl = true;\n  context.safe = true;\n  EXPECT_TRUE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  context.platform = \"macOS\";\n  EXPECT_TRUE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  for (const char* platform : {\"\", \"Linux\", \"Android\"}) {\n    context.platform = platform;\n    EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  }\n  context.platform = \"Windows\";\n  context.angle_is_swiftshader = false;\n  EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  context.angle_is_swiftshader = true;\n  context.shared_context_is_gl = false;\n  EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n}\n\nTEST(ProfileSoftwareWebGPUPolicyTest, RetainsSafetyAndExplicitAdapterChoices) {\n  webgpu::ProfileSoftwareWebGPUContext context;\n  context.platform = \"Windows\";\n  context.angle_is_swiftshader = context.shared_context_is_gl = context.safe = true;\n  context.safe = false;\n  EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  context.safe = true;\n  context.has_dawn_overrides = true;\n  EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  context.has_dawn_overrides = false;\n  context.force_compat = true;\n  EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  context.force_compat = false;\n  for (auto adapter : {WebGPUAdapterName::kD3D11, WebGPUAdapterName::kOpenGLES}) {\n    context.requested_adapter = adapter;\n    EXPECT_FALSE(webgpu::CanUseProfileSoftwareWebGPU(context));\n  }\n  context.requested_adapter = WebGPUAdapterName::kSwiftShader;\n  EXPECT_TRUE(webgpu::CanUseProfileSoftwareWebGPU(context));\n}\n\nTEST(ProfileSoftwareWebGPUPolicyTest, OnlyApprovedCPUReasonIsIgnored) {\n  auto make_info = [](WGPUAdapterType type, uint32_t vendor) {\n    WGPUAdapterInfo raw = {};\n    raw.backendType = WGPUBackendType_Vulkan;\n    raw.adapterType = type;\n    raw.vendorID = vendor;\n    raw.deviceID = 0xc0de;\n    raw.description = {\"\", 0};\n    raw.architecture = {\"\", 0};\n    return raw;\n  };\n  // Match this file's existing C-ABI fixture pattern. Fully initialize each\n  // independent POD value before creating a read-only, layout-compatible view.\n  // No immutable C++ output object is mutated or made to own these strings.\n  auto view = [](const WGPUAdapterInfo& raw) -> const wgpu::AdapterInfo& {\n    return *reinterpret_cast<const wgpu::AdapterInfo*>(&raw);\n  };\n  const WGPUAdapterInfo approved = make_info(WGPUAdapterType_CPU, 0x1ae0);\n  const auto& info = view(approved);\n  auto denied = webgpu::ProfileSoftwareWebGPUBlocklistOptions(false, info);\n  EXPECT_EQ(denied.ignores, WebGPUBlocklistReason::None);\n  EXPECT_TRUE(gpu::IsWebGPUAdapterBlocklisted(info, denied).blocked);\n  auto admitted = webgpu::ProfileSoftwareWebGPUBlocklistOptions(true, info);\n  EXPECT_EQ(admitted.ignores, WebGPUBlocklistReason::CPUAdapter);\n  EXPECT_FALSE(gpu::IsWebGPUAdapterBlocklisted(info, admitted).blocked);\n  admitted.blocklist_string = \"1ae0:c0de\";\n  EXPECT_TRUE(gpu::IsWebGPUAdapterBlocklisted(info, admitted).blocked);\n  const WGPUAdapterInfo other_cpu = make_info(WGPUAdapterType_CPU, 0x1002);\n  EXPECT_EQ(webgpu::ProfileSoftwareWebGPUBlocklistOptions(true, view(other_cpu)).ignores,\n            WebGPUBlocklistReason::None);\n  const WGPUAdapterInfo physical = make_info(WGPUAdapterType_DiscreteGPU, 0x1ae0);\n  EXPECT_EQ(webgpu::ProfileSoftwareWebGPUBlocklistOptions(true, view(physical)).ignores,\n            WebGPUBlocklistReason::None);\n}\n\nTEST(ProfileSoftwareWebGPUPolicyTest, ExclusiveDescriptorParticipatesInIdentity) {\n  dawn::native::DawnInstanceDescriptor normal;\n  dawn::native::DawnInstanceDescriptor exclusive;\n  EXPECT_TRUE(normal == exclusive);\n  exclusive.onlyUseSwiftShader = true;\n  EXPECT_FALSE(normal == exclusive);\n}\n\nTEST(ProfileSoftwareWebGPUPolicyTest, InvalidExclusivePathsNeverDiscoverAdapters) {\n  const char* relative[] = {\"relative/\"};\n  const char* multiple[] = {\"/first/\", \"/second/\"};\n  for (unsigned variant : {0u, 1u, 2u}) {\n    dawn::native::DawnInstanceDescriptor native_desc;\n    native_desc.onlyUseSwiftShader = true;\n    native_desc.additionalRuntimeSearchPathsCount = variant;\n    native_desc.additionalRuntimeSearchPaths = variant == 1 ? relative : multiple;\n    wgpu::InstanceDescriptor desc;\n    desc.nextInChain = &native_desc;\n    dawn::native::Instance instance(&desc);\n    wgpu::RequestAdapterOptions options;\n    options.backendType = wgpu::BackendType::Vulkan;\n    options.forceFallbackAdapter = true;\n    EXPECT_TRUE(instance.EnumerateAdapters(&options).empty());\n  }\n}\n\nTEST(ProfileSoftwareWebGPUPolicyTest, ExclusiveInstanceCannotProbeOtherBackends) {\n  const char* paths[] = {\"/not-probed/\"};\n  dawn::native::DawnInstanceDescriptor native_desc;\n  native_desc.onlyUseSwiftShader = true;\n  native_desc.additionalRuntimeSearchPathsCount = 1;\n  native_desc.additionalRuntimeSearchPaths = paths;\n  wgpu::InstanceDescriptor desc;\n  desc.nextInChain = &native_desc;\n  dawn::native::Instance instance(&desc);\n  wgpu::RequestAdapterOptions options;\n  options.backendType = wgpu::BackendType::OpenGLES;\n  EXPECT_TRUE(instance.EnumerateAdapters(&options).empty());\n}\n#endif  // BUILDFLAG(IS_LINUX)\n\n}  // namespace gpu\n"
    },
    {
      "path": "gpu/apostate_dawn_software_check.cc",
      "before": "  native_desc.additionalRuntimeSearchPaths = paths;\n",
      "after": "  native_desc.additionalRuntimeSearchPaths = paths;\n  native_desc.onlyUseSwiftShader = true;\n"
    }
  ],
  "new_files": {
    "gpu/command_buffer/service/profile_software_webgpu_policy.h": "scripts/templates/software-webgpu-admission/policy.h"
  }
}
